fastapi>=0.95.2
uvicorn[standard]>=0.22.0
motor==3.7.1
pydantic>=1.10.10
python-dotenv>=1.0.0
# NOTE: do NOT include the standalone "bson" package — use the bson from pymongo
# Optional for typing support in editors
types-python-dateutil>=2.0.0