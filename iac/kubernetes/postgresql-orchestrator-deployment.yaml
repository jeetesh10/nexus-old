apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgresql-orchestrator
  namespace: default
  labels:
    app: postgresql-orchestrator
spec:
  replicas: 2
  selector:
    matchLabels:
      app: postgresql-orchestrator
  template:
    metadata:
      labels:
        app: postgresql-orchestrator
      annotations:
        linkerd.io/inject: enabled
    spec:
      containers:
      - name: postgresql-orchestrator
        image: nexus/postgresql-orchestrator:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        env:
        - name: POSTGRESQL_URL
          value: "postgresql://postgres:postgres@postgresql-service:5432/postgres"
        - name: AUTH_API_URL
          value: "http://auth-api-service:8080"
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            memory: "256Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 20
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: postgresql-orchestrator-service
  namespace: default
  labels:
    app: postgresql-orchestrator
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: postgresql-orchestrator
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: postgresql-orchestrator-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: postgresql-orchestrator.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: postgresql-orchestrator-service
            port:
              number: 8000
