apiVersion: v1
kind: ConfigMap
metadata:
  name: mongodb-orch-config
  namespace: default
data:
  # Point to the in-cluster MongoDB service (change host if different)
  MONGO_URI: "mongodb://mongodb-service.default.svc.cluster.local:27017/?authSource=admin"
  MONGO_DB: "nexus"
  PORT: "8080"